# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Git workflow

- Commit after each meaningful, working chunk of work — don't let large amounts of uncommitted work pile up.
- Write clean, descriptive commit messages that explain *why*, not just *what*.
- Push to GitHub (`origin`) after committing, so work is backed up remotely, not just local.
- Before any destructive git operation (reset --hard, checkout that discards changes, force-push), check `git status` first and confirm with the user.
- Feature work happens on `claude/zau-region-restructure-pv5c8n`, PR'd into `main`. If that branch's PR has already merged, restart it from latest `main` before starting new work rather than stacking on merged history.
- Once a chunk of work is committed and pushed, **open the PR and merge it automatically** — don't wait for the user to say "pr n merge" each time. Still verify `mergeable_state` is clean before merging, and confirm the deploy workflow succeeds afterward, same as always.

## Project: Zau Region

A fan-made Pokémon RPG. Started as a single 1638-line HTML file; the user's stated ambition is for this to become a genuinely huge game ("hours and hours of gameplay," a project spanning weeks/months) — not a short browser-game arc. Depth over breadth: flesh out what exists before adding more.

### Two parallel project directories

- **`zau-region-phaser/`** — the Phaser 3 version. **This is now the live production build** — `.github/workflows/deploy-zau-region.yml` builds and deploys it (path-filtered to `zau-region-phaser/**`) as of the Phase 5 cutover. This is where all new work happens.
- **`zau-region/`** — the original Vite + vanilla JS/DOM/CSS version. Content-frozen, no longer deployed, but **not deleted** — still in the repo for reference (worldbuilding docs, real game data). Deleting it outright is a separate, more consequential decision than the cutover itself; don't do it without an explicit ask.

Full migration plan (context, decisions, phase breakdown) lives at `/root/.claude/plans/project-zau-region-enumerated-treehouse.md` in this environment — not checked into the repo. Key points if that file isn't available:
- Phase 1 (done): scaffolded `zau-region-phaser/`, ported `data/*.js`/`state.js`/`save.js` verbatim, one walkable scene tied to real `state.pos`, a `window.__zauTest` bridge for Playwright testing.
- Phase 2 (done): `battleEngine.js` emits `render`/`end` events instead of touching the DOM; `BattleScene` consumes them.
- Phase 3 (done): a real CC0 tileset is in — `kenney.nl` itself is blocked by this environment's network policy, so the tiles were pulled from `github.com/ETdoFresh/kenney.nl` (a verified CC0 mirror of Kenney's actual "Roguelike/RPG pack") into `zau-region-phaser/public/tiles/`. `TownScene`/`LabScene`/`TrailScene`/`LeagueScene` are all built and Playwright-validated (real tiles, wild encounters, trainer battles, League gating). `TitleScene`/`CutsceneScene`/`CharCreateScene` close out the phase — the game now boots into a real title screen (Continue/Begin Journey/Skip Intro) instead of straight into Home with a blank name.
- Phase 4 (done): Party/Bag/Mart/Center/Pokédex/move-learn all built as overlay scenes (launched on top of the calling scene, never pausing it — closing is `scene.stop()`), wired into Town/Trail/League's action bars and Battle's Switch/Bag/Flee row. Playwright-validated including a mid-battle item use and a forced move-learn prompt.
- Phase 4.5 (done): real fade transitions (`transitions.js`'s `goToScene`/`fadeIn`) on every scene switch instead of hard jump cuts, plus a real loading screen (progress bar) in `BootScene` — infrastructure for when the asset list actually grows, not just decoration for today's near-instant load. The DOM version's animated storm/lightning title-screen effects are deliberately not rebuilt yet — that's tied up with the not-yet-picked real game title and the eventual GBA-style art pass, revisit together later.
- Phase 5 (done): cutover — production Pages deploy now builds `zau-region-phaser/`. `zau-region/` stays in the repo, undeployed.
- Phase 6 (done): fixed the "feels like an arcade game" complaint — `mapRenderer.js`'s `createWalker` now polls held arrow keys every frame (continuous movement, one tile per 140ms while held) instead of one tile per keypress, plus a squash/stretch tween as a placeholder walk-cycle. Town/Trail/League got a real zoomed (1.7x) follow-camera (`setupFollowCamera`) instead of showing the whole map at once. Needed a second, unzoomed HUD camera (`setupHUD`) since `setScrollFactor(0)` only cancels camera pan, not zoom — without it, HUD text/buttons under a zoomed main camera render at the wrong screen scale/position.
- Phase 7 (done): real character sprites + full customization, replacing the emoji-avatar picker. Layered LPC (Liberated Pixel Cup) sprite art — body/skin tone, hairstyle, hair color, outfit as four stacked Phaser sprites in a container (`src/playerSprite.js`), each swappable independently and all sharing the same 64x64-frame/13-col universal animation sheet layout so they always line up. A real 4-direction 8-frame walk cycle (`WALK_ROW`/`WALK_FRAMES` in `src/data/appearance.js`) replaced the old squash/stretch placeholder tween. `CharCreateScene` is a tabbed picker (Skin/Hair/Hair Color/Outfit) with a live preview that rebuilds on every pick. Curated subset copied into `zau-region-phaser/public/character/`: 7 skin tones, 8 hairstyles × 8 colors, 5 outfit presets (torso+legs pairs). **This art is not CC0 like the tileset** — it's CC-BY-SA 3.0/GPL 3.0/OGA-BY 3.0 depending on the specific asset, sourced via the Universal LPC Spritesheet Character Generator (github.com/sanderfrenken/Universal-LPC-Spritesheet-Character-Generator, itself built from OpenGameArt.org's Liberated Pixel Cup collection) — per-asset author credits are recorded in `zau-region-phaser/public/character/CREDITS.md`. **Still owed**: an in-game Credits/About screen surfacing that attribution (currently only exists as a repo file) before this is fully license-compliant for a wider public audience.
- Phase 8 (done): party cap (6, `MAX_PARTY` in `state.js`) + PC Box storage, and the four real status conditions (burn/poison/paralyze/sleep — the ones reachable from `data/moves.js`'s current move roster; freeze/confusion aren't wired up since no move triggers them yet, not a missing feature so much as unreached data). Catching past a full party now routes to `state.box` (unlimited) instead of being blocked or forced; `PartyScene` got a Party/Box tab switcher with deposit/withdraw and pagination (6 rows/page). Status: burn/poison tick 1/16 and 1/8 maxHP at end of turn, burn halves physical damage dealt, paralysis has a 25% full-para chance and halves effective speed for turn order, sleep blocks acting for 1-3 turns; a mon can hold only one status at a time, matching the real games, and fainting always clears it. `moveFor`-registry moves that inflict status use real per-move chances (Ember/Flamethrower 10% burn, Thunder Shock 10% paralyze, Dragon Breath 30% paralyze, Poison Sting 30% poison, Hypnosis always sleep). The Pokémon Center cures status along with HP; catching gets the real games' sleep/status catch-rate bonus. `DexScene` now lists Box mons too (marked "In Box") so a catch doesn't disappear from the roster just because it got stored.
- Phase 9 (done): abilities + held items — real Pokémon mechanics, not invented. `data/abilities.js` assigns every species (same species-list precedent as `data/baseStats.js`) its actual in-game ability; most are flavor-only (`effect: null`, still real, just not one of the handful wired up mechanically), 13 are hooked into `battleEngine.js`: Overgrow/Blaze/Torrent/Swarm (+ Verdanyx's custom Verdant Surge) boost same-type moves, Levitate/Flash Fire are real full immunities (Flash Fire also buffs the holder's own Fire moves for the rest of the battle), Static/Flame Body have a 30% on-contact status chance against the attacker, Synchronize mirrors burn/poison/paralyze (not sleep, matching the real games) back onto whoever inflicted it, Guts boosts Attack when statused and negates burn's own damage penalty, Sturdy survives a lethal hit from full HP, Shed Skin has a 1-in-3 end-of-turn chance to cure any status. Held items (`data/items.js`, `category: "held"`): 4 real type-boost items (Charcoal/Mystic Water/Miracle Seed/Magnet/Black Belt, +20% to matching-type moves), Leftovers (1/16 maxHP heal every turn), Lum Berry (cures any status the instant one lands, single-use). `BagScene` gained a Give flow (equips onto a party mon, swapping out whatever it already held); `PartyScene` shows ability + held item per mon with a Take button; `DexScene`/`BattleScene` surface ability name too. All of this is real infrastructure for Mega Evolution (a held Mega Stone + a battle trigger) — that's the next natural step once picked up.
- Phase 10 (done): non-starter evolution + 3 new evolutionary lines, specifically seeding real Mega-Evolution-eligible final species before Mega Evolution itself gets designed (the user's call — know the real roster before picking stones). Before this, only STARTER_CHAINS mons could evolve at all; a caught wild mon's species was permanently fixed. `data/evolutions.js` (new) is a species-keyed chain for non-starter mons, real level thresholds where the games use level-up. Two of the six finished lines are real **trade** evolutions (Kadabra→Alakazam, Haunter→Gengar) — no trade partner exists in single-player, so these use the **Linking Cord**, the actual item Pokémon Legends: Arceus/Scarlet & Violet introduced for exactly this (`data/items.js`, `category: "evolution"`, sold at the same Mart tier as Leftovers). `mon.js`'s `evolveIfReady` now branches for both starter and non-starter mons (called unconditionally from `levelUpMon`, was gated on `mon.key` before); a new `evolveWithItem(mon, itemKey)` handles the Linking Cord path, wired into a `BagScene` picker (`renderEvolvePicker`/`applyEvolveItem`) parallel to the existing medicine-target and held-item-give pickers. Three new catchable base species added to `WILD_SPECIES`/`WILD_ZONE_TABLE.outskirts` — Riolu, Gible, Absol — chosen the same way, real Pokémon whose lines end in a real Mega-eligible species. The six finished lines: Magikarp→**Gyarados**, Abra→Kadabra→**Alakazam**, Gastly→Haunter→**Gengar**, Riolu→**Lucario**, Gible→Gabite→**Garchomp**, and Absol (already final-stage, no evolution needed). All real base stats/dex IDs/abilities for the 9 new species (3 catchable bases + 6 evolved forms) — same `data/baseStats.js`/`data/spriteIds.js`/`data/abilities.js` precedent as everything else.
  - **Explicit long-term ambition from the user, not scoped yet**: eventually grow the Pokédex to ~300-400 species with more Mega-eligible lines, including newer ones from Pokémon Legends Z-A. Revisit as its own future content pass — this phase was deliberately the minimum needed to unblock Mega Evolution's design, not that larger expansion.
- Phase 11 (done): **Mega Evolution** — the game's signature gimmick, built on real data. `data/megas.js` holds each of the 6 eligible species' actual Mega form: real Mega base stats (always +100 BST, HP unchanged), real Mega type (e.g. Gyarados → Water/Dark), real Mega ability, and PokeAPI's real form IDs (10037+) for official Mega artwork. Real rules: the player needs the **Key Stone** (`state.hasKeyStone`, granted by the story when Dario is beaten), the active mon must hold its own species' Mega Stone (`data/items.js`, `effect: "mega_stone"`, sold at the top Mart tier), and it's **once per battle** (`state.battle.megaUsed`). Transforming doesn't cost the turn — a Mega button on the battle action bar (only lit when `engine.canMegaEvolve()` is true) transforms immediately, then the player picks a move as normal. `mon.js`'s `applyMega`/`revertMega` swap type/ability/five stats in place and recompute from Mega base stats; `currentMonDisplay` and `statsForMon` are Mega-aware (name/type/sprite change for display, `species` stays the base species so lookups keep working). Reverts on faint, win, loss, and flee — and defensively at battle start. Of the six Mega abilities only Adaptability is mechanical (real 2x STAB), the rest flavor-only, same precedent as `data/abilities.js`. Trainer/rival Megas (e.g. Dario or Vance Mega Evolving) are a natural future addition — the engine is player-only for now.
- Phase 12 (done): **Harbor District** — the first real district map, and the template for the rest. Built straight from `zau-region/districts/harbor.md` (stratum 3 in `WORLD.md`): `HARBOR_MAP` in `data/maps.js` (a flat 9×7 dock shelf ringed by water tiles — blocked tiles render as `water`), `HarborScene` with six step-triggered sub-locations from the design doc — **Coral's Reef Gym** (starts the real `ctx: 'league'` battle for leader index 0, so Coral is fightable from her own district; the League hub still lists her and both share `state.leagueBeaten[0]`), the **Fish Market** (launches the Mart overlay), and Cargo Row / Ferry Terminal / Lighthouse Point / Drowned Stair as flavor toasts carrying the doc's story hooks until their systems exist. Entered from a new **Harbor Steps** exit on the Town map (`TOWN_MAP.harborX/harborY`), gated on `state.darioBeaten` per the doc's post-Act-1 level band. Its own wild table (`WILD_ZONE_TABLE.harbor`, was a leftover stub) now spawns the doc's real Water-district roster — Wingull, Tentacool, Krabby, Horsea, Chinchou, Buizel, Pelipper (rare) + Magikarp — with real stats/dex IDs/abilities, plus the real Wingull→Pelipper evolution (Lv.25). `state.pos.harbor` is defaulted in `HarborScene.init` too, since `Object.assign` replaces the whole `pos` object on loading an older save. **Pattern for the next districts** (Ember Quarter, Greenline Terraces, Signal District, Undercity — all "named only" in `WORLD.md`): a district `.md` design doc first, then map + scene + zone table + exit gating, exactly like this one.
- Phase 13 (done): **Ember Quarter** — second real district, built with the exact Harbor pattern (design doc → map + scene + zone table + exit gating). `zau-region/districts/ember.md` (new) is the design doc: Ashgrave's Foundry Gym, the Scrapyard Exchange (Mart), the Kilns (Foreman Kettering), the Meridian Substation, the Freight Lift (locked toward Greenline Terraces), the Harbor Ramp. Its story hook is deliberately a *different* local oddity from Harbor's cargo thread — power moving with nothing to power (the substation bleeds the Quarter's grid, and its trunk cable runs *down*, feeding Vance's excavation) — per STORY.md's "each district plants its own hook" rule. `EMBER_MAP` (9×9, brick mill blocks splitting three haul-roads, `floorKey 'dirt'`/`blockedKey 'wallBrick'`) + `EmberScene` (gym = league battle idx 1, shared `state.leagueBeaten[1]` with the hub). Reached from a new Harbor Ramp tile on the Harbor's east edge (`HARBOR_MAP.rampX/rampY`, layout cell (8,2) opened), gated on Coral's badge; leaving resets `state.pos.ember` to the spawn. New real roster (`WILD_ZONE_TABLE.ember`): Slugma, Numel, Aron, Rolycoly, Litwick, Torkoal, Magnemite, rare Houndour, with real stats/dex IDs/abilities (Flame Body/Sturdy/Flash Fire are the mechanically wired ones) and real level-up evolutions (Magcargo, Camerupt, Lairon→Aggron, Carkol→Coalossal, Lampent, Magneton, Houndoom). Lampent→Chandelure and Magneton→Magnezone are real *stone* evolutions with no stone item yet — those lines stop at their middle stage until evolution stones exist. Three of the lines end in real Mega-eligible species, so **Mega Aggron / Mega Camerupt / Mega Houndoom** were added to `data/megas.js` (real stats/types/abilities, PokeAPI form IDs 10053/10087/10048 verified visually) with Aggronite/Cameruptite/Houndoominite in the Mart's badge-5 tier — the Mega roster is now 9. Three real moves added to `data/moves.js` for the learnsets (Smog 40% poison, Astonish, Smack Down). Playwright-validated: ramp gate blocks then opens, Exchange opens Mart, gym runs `ctx:'league'` vs Leader Ashgrave and sets `leagueBeaten[1]`, wild table spawns Aron/Rolycoly/Slugma, ramp exit returns to Harbor with the spawn reset.
  - **Real bug found by this phase's test and fixed in `transitions.js`**: overlay scenes (Party/Bag/Mart/Center/Dex) never pause the scene under them, so walking off a shop tile into a battle left the Mart rendered on top of `BattleScene`. `goToScene` now stops any active overlay before transitioning — an overlay belongs to the scene that launched it.
  - **Next districts** in the same pattern: Greenline Terraces (Thistle, Grass/Bug/Fairy — reached via the Ember Freight Lift, gate on Ashgrave's badge), then Signal District (Prism), then the Undercity (Obsidian — where Harbor's Drowned Stair and Ember's sealed service tunnel converge). `testBridge.js`'s `getScene` returns only `{key, active}`, so tests can't read a scene's toast text — verify flavor spots by position/screenshot.
- Phase 14 (done): **Greenline Terraces** — third district, same pattern. `zau-region/districts/greenline.md` (new): Thistle's Canopy Gym, the Seed Bank (Mart), the Allotments, Meridian Irrigation Works, the Overlook (Old Sato), the Service Stair (locked toward Signal District), the Freight Lift down. Story hook = *water* (Harbor=cargo, Ember=power): the Meridian pump intake draws from the old tunnels, and the plants nearest it grow wrong — the softest of the three hooks on purpose, folklore until Act 3. `GREENLINE_MAP` (9×9, four hedge-row terraces with offset gaps so the walk zigzags up, `floorKey 'grass'`/`blockedKey 'tree'`) + `GreenlineScene` (gym = league idx 2). Reached from the Ember Freight Lift (`EMBER_MAP.liftX/liftY`, gated on Ashgrave's badge); leaving resets `state.pos.greenline`. Roster (`WILD_ZONE_TABLE.greenline`): Oddish, Hoppip, Sewaddle, Combee, Flabébé, Ralts, Cutiefly (shared), uncommon Heracross, rare Pinsir — real stats/dex IDs/abilities, real level-up evolutions (Gloom, Skiploom→Jumpluff, Swadloon→Leavanny, Vespiquen, Floette, Kirlia→Gardevoir). Gloom→Vileplume/Bellossom and Floette→Florges are real *stone* evolutions with no stone item yet. **Mega Gardevoir / Mega Heracross / Mega Pinsir** added (IDs 10051/10047/10040 verified visually) with their stones — Mega roster now 12. Eight real moves added (Absorb, Acid, Razor Leaf, Gust, Vine Whip, Disarming Voice, Horn Attack, Vise Grip). Playwright-validated end to end (lift gate, Seed Bank → Mart, Thistle league battle with no overlay leak, wild Oddish/Ralts/Heracross, lift exit).
  - **Evolution stones are now the obvious missing item family** — four lines across Ember/Greenline stop short of a real final form (Chandelure, Magnezone, Vileplume/Bellossom, Florges) for lack of Dusk/Thunder/Leaf/Sun/Shiny Stones. Same `category: "evolution"` slot as the Linking Cord; a contained follow-up.
  - **Next districts** in the same pattern: Signal District (Prism, Electric/Steel — via the Greenline Service Stair, gate on Thistle's badge), then the Undercity (Obsidian). **But see the direction change below before building them.**

### Direction change (2026-09-12, user) — story/cast/presentation before more districts

After Phase 14 the user laid out what "a real Pokémon game" needs beyond gym → next city → gym, in their words the reasons a game can have "hours of gameplay":
- **Characters and a full plot first.** Decide the cast *before* building more places so NPCs can be placed with real dialogue as each district is built, rather than filled in after. Main-story beats *between* gyms (not just gym-to-gym), plus side stories/side quests. The story bible (`STORY.md`/`CHARACTERS.md`) has the spine; what's missing is a beat sheet and a district-by-district cast.
- **When does Mega Evolution unlock?** Not "from the jump." The user's reference point: real games hand it over around when the team is in its thirties. Currently the Key Stone is granted on beating Dario (end of Act 1, team ~Lv.16-20) and stones are simply bought at badge tier 5 — both need re-homing into story beats. Open design question, not decided yet.
- **Presentation has to go up a level**: real screens, loading, cutscenes, interfaces, pop-ups, banners, side notes, and *animations* instead of static images — "a real real game." Cutscenes exist only as the intro's text slides (`CutsceneScene`); NPCs exist only as toast text on tiles; battles have no attack/hit/HP animations; there's no dialogue-box system, no area-name banner, no evolution/catch/Mega animation.
- Practical consequence: the next work is (1) a story + cast + Mega-unlock design pass in the bible, (2) an NPC/dialogue/cutscene engine, (3) a presentation/animation pass — and only then more districts, built with their cast and beats in hand. Retrofit the three built districts' toast-NPCs onto the real NPC system when it exists.
- **User's answers (2026-09-12)**: cast now; a full, complete story for both plot and region; Key Stone **after Badge 3 with a story-given Absol** that has "good IVs" (→ a real IV system is the honest way to deliver that, see `MEGA.md`); other stones via side quests, caves/tunnels, big story moments, some required; order = story+cast → NPC/dialogue engine (for *relevant, important* characters, not just filler) → animation pass (= the visual upgrade, one and the same).

### Phase 15 (done, docs only): the story and cast pass

`zau-region/STORY.md` is now the **complete plot** — the "deep resonance" idea that makes Mega Evolution the answer to the story's central problem (a Mega is transformation *with* a bond and a revert; Verdanyx is the same energy *without* either), the full timeline including what really happened to Dario's mother (**Elena Voss is alive**, living in the Old Lines as "the Warden," keeping Verdanyx calm for 18 years; her Absol is the messenger and the gift Pokémon; her Key Stone is the player's), and an **act-by-act beat sheet** with at least one main-story beat between every gym (Harbor: Cargo Row break-in; Ember: the blackout + Boiler Tunnels dungeon; Greenline: the storm on the Overlook; the Key Stone beat K1–K5; Signal: the pulse; Undercity: the Warden; Sprawl: Dario's choice; Tower: Vance, who activates the rig even after losing; Underlight: the battle is stopping the rig, the ending is a *revert*, not a defeat). Postgame is the Skyline with Verdanyx at rest and catchable. A "who knows what, when" table keeps dialogue from leaking the ending. `CHARACTERS.md` has the **full cast**: main cast with role/voice/"says when" (Alma, Priya, Dario, Elena, Vance, Mabosso, and new **Dr. Ines Halloran**, Meridian's community-relations director who is kind to Dario and genuinely doesn't know), plus a named cast for every district (Rossi, Maren, Ambrose, Kettering, Ozren, Old Sato, Kess, Prism's Juno, Obsidian, Kestrel, Halvard, Sloane, Amara, Iven…) and a trainer-class table. `MEGA.md`: the Key Stone moves to after Badge 3 (code still grants it on Dario — placeholder to change), the gift Absol spec, and a **source for all 12 stones** (2 story-given, 5 side quests, 2 caves, 3 postgame rematch/Skyline; none sold — the Mart tier-5 stones are a placeholder to remove). `SIDEQUESTS.md`: 10 side quests with rewards, rematches, the dungeon list, cosmetics, and the systems they assume (quest flags, fishing, evolution stones, field moves, IVs, rematch scaling). `districts/signal.md`, `undercity.md`, `sprawl.md`, `tower.md`, `skyline.md` give every remaining stratum a design doc with real Mega-eligible wild lines (Ampharos/Manectric, Sableye/Banette/Mawile, Audino/Kangaskhan, Altaria/Salamence). `WORLD.md` gained the player's full route table with gates and level bands, and the "one hole" geography rule (every district's Meridian installation runs into the same tunnel network).
- Phase 16 (done): **the NPC / dialogue / cutscene engine**, plus the first real story beats running on it.
  - `src/dialogue.js` — `DialogueScene` (key `'Dialogue'`), a JRPG text box as an overlay scene: name plate, typewriter text (16ms/char), multi-page on Space/Enter/Z/click, a choice list with ▶ cursor (Up/Down + confirm, or click/hover). Promise API: `await say(scene, name, lines)`, `const i = await choose(scene, name, prompt, options)`. In `transitions.js`'s `OVERLAY_SCENES` so a scene transition closes it.
  - `src/lock.js` — a counted shared `inputLock`; `createWalker` skips arrow polling while locked, NPC interaction is ignored while locked. Every dialogue/script holds it.
  - `src/script.js` — `runScript(scene, steps, actors)`: steps `say / choice(with nested then) / if(state)→then/else / set(flag) / give / money / heal / walk(actor, 'LURD' path) / face / wait / pan / flash / shake / call / battle`. `battle` ends the script (`goToScene('Battle', {kind:'trainer', trainerKey, returnTo})`); the trainer's `winFlag` is how a beat continues after the fight. Scripted walks bypass the walker's `onStep` on purpose (no encounter rolls in cutscenes). The scene-left guard checks `sys.settings.status` for SHUTDOWN/DESTROYED, **not** `isActive()` — a cutscene launched from `create()` runs while the scene is still CREATING.
  - `src/npcs.js` — `preloadNPCLayers`, `makeActor` (ctrl + pos + `moveTile`), `placeNPCs(scene, {npcs, offsetX, offsetY, player, walker, posRef})` → `{actors, npcs, isBlocked, run}`. NPCs are `createPlayerSprite` containers (same LPC layers as the player), block their tile via the walker's new `isBlocked` hook, y-sorted by depth every frame, and turn to face the player when spoken to. Talk = Space/Enter/Z while **facing** the NPC (`walker.facing` = last attempted direction) or click an adjacent one. `def.when(state)` decides presence per beat; `def.script(state)` returns steps ("says when").
  - `src/story.js` — `state.story` / `state.quests` (new defaults in `state.js`, `ensureStoryState()` guards old saves), `hasFlag/setFlag/badgeCount`.
  - `src/data/trainers.js` + battle ctx `'trainer'` — generic NPC trainers (`startTrainerBattle(ctx, trainerKey)`, `state.battle.trainerKey`, reward from the trainer, `winFlag` set on win). Two so far: `darioHarbor` (rival rematch, Lv.20–21, winFlag `harborDario`) and `siteSecurityHarbor` (Meridian Site Security Pell, winFlag `cargoRow`).
  - `src/data/npcs.js` — every placed character so far, straight from `CHARACTERS.md`: Home **Alma** (+ `HOME_INTRO`, the game's first on-map cutscene: Alma walks over from the counter, a choice, the bag line — runs once on a fresh game via `story.introDone`), Town **Priya** (beside the lab, `beside()` helper finds the floor tile), Harbor **Rossi / Maren / Ambrose / Dario / Priya**, Ember **Kettering / Ozren / Bo / Halloran** (present from Badge 1), Greenline **Old Sato / Dr. Kess / Wren**. **Placement rule**: never in a one-tile corridor — the Greenline's rows are exactly that, so there the NPCs stand ON their landmark tiles and carry the description (talking to Wren opens the Seed Bank via a `call` step).
  - **Act 2 Harbor beats H1–H3 are live**: Maren's tip (`marenTip`) → Dario's rematch at Cargo Row (`harborDario`) → stepping on Cargo Row runs `HARBOR_CARGO_BREAKIN` (Dario, a camera shake, Site Security) → `cargoRow`; afterwards Dario leaves, Priya appears by the lighthouse with the "they come from under" line, Rossi/Maren/Ambrose/Halloran all have post-beat lines. Playwright-validated end to end incl. flags surviving reload (`test-npcs.mjs`).
  - **Still toast-based** (unchanged): the landmark step-triggers without a person (Ferry Terminal, Drowned Stair, Freight Lift, Service Stair, Allotments, gym doors). Fine as place descriptions; the *people* are NPCs now.
- Phase 17 (done): **Ember beats E2–E3 and Greenline beats G2–G3 on the engine**, including the first dungeon and the first weather cutscene.
  - **Ember**: Halloran (or Dario, new `darioEmber` NPC at (6,3)) sets `emberHalloran` once the Harbor beat is done; talking to Kettering then runs `EMBER_BLACKOUT` (the Quarter goes dark mid-sentence — `EmberScene.setBlackout()` tweens a world-space tint rect; flash + shake) → `blackout`. The Kilns tile (1,5) then leads into **`BoilerScene`** (`BOILER_MAP`, 5×12 Trail-style corridor, `floorKey 'dirt'`/`blockedKey 'wallBrick'`, `WILD_ZONE_TABLE.boiler` = the Ember roster's cave-dwellers at 15%, a dark wash rect while the power's out). Two **Mill Worker** trainers (`millIla`, `millDev` in `data/trainers.js`) stand in one-tile chokepoints (rows 8 and 4 are `[1,1,0,1,1]`) and leave once beaten (`when: !flag`). The **Aggronite** sits in the alcove at (3,1) (`BOILER_STONE`, `aggroniteFound`, per `MEGA.md`'s cave-stone plan). The breaker at (2,0) runs `BOILER_BREAKER` → `lineRestored` and returns to Ember with the lights back; Kettering's E3 payoff line ("Runs *down*. So who's drawing?"); Dario then takes Halloran's sponsorship (`emberDarioPin`) and leaves the Quarter.
  - **Greenline**: Priya stands on the Allotments tile from Badge 2 (`priyaGreen`, sets `priyaSamples` with the cuttings + the Absol sightings). Old Sato then runs **`GREENLINE_STORM`**: tint in (`stormStart`), lightning flashes + shakes, `showAbsol()` places a real Absol sprite (PokeAPI #359 via `loadMonSprite`, 🐺 fallback) on a new terrace-edge tile (`GREENLINE_MAP.edgeX/edgeY` = (8,5), row 5 opened east), the camera pans to it (`pan` step's `toEdge` uses the scene's `panTarget()` and `cam.stopFollow()`; `stormEnd` re-follows), it looks at the player, fades; → `stormSeen`. Priya's and Sato's lines change after.
  - **Three real bugs the screenshots caught, fixed**: (1) a cutscene started on top of an open Mart played over it — `runScript` now calls `closeOverlays(scene, {keepDialogue:true})` (new export in `transitions.js`; `goToScene` uses it too). (2) Any object created *after* `setupHUD()` isn't in the HUD camera's ignore list and gets drawn twice (once zoomed by the main camera, once unzoomed by the HUD camera) — the storm Absol showed up as two. Scenes that create objects at runtime must keep `this.hudCam = setupHUD(...)` and call `this.hudCam.ignore(obj)`. (3) `add.rectangle(x,y,w,h,color, 0)` sets the *fill* alpha to 0, so tweening the object's `alpha` never shows anything — tint rects are created with fill alpha 1 and `.setAlpha(0)`.
  - Test driver pattern for cutscenes (`test-beats.mjs`): press Space whenever the Dialogue scene is active until a story flag flips, since scripts have non-dialogue waits between pages.
- Phase 18 (done): **IVs + the Key Stone beat (K1–K4)** — Mega Evolution now unlocks where the story says it does.
  - **IV system** (`mon.js`): `rollIVs()` (0–31 per stat), `perfectIVs()`, `ivsFor(mon)` (old saves' mons have no `ivs` → treated as all-zero, which is exactly what their stats were computed with — nothing shifts on load), `ivSummary(mon)` → `{total, word}` with the real games' judge phrasing (Outstanding ≥151, Very good ≥121, Pretty good ≥91, Decent ≥61, Average). `computeStats(base, level, ivs)` is the real formula with IVs (EV=0, neutral nature). Rolled at creation everywhere: starters, wilds, trainer mons (`buildBattleMon`), gifts (`makeGiftMon`); a catch keeps the wild's roll. Party rows show `IV <total> (<word>)`, Dex rows `IV <total>`.
  - **Key Stone beat** (`data/npcs.js` `KEYSTONE_BEAT`, `src/keystone.js` `grantKeyStone()`): after Badge 3 with `lineRestored`, the storm Absol waits at the Meridian keycard door in the Boiler Tunnels (`DOOR` = (1,0), beside the breaker; a placed sprite via the new `addMonSpriteAt()` in `spriteLoader.js`, which Greenline's storm now uses too). Stepping on the door or breaker runs the beat: the keycard, the door, the original excavation's cache, E. VOSS's locker, the note, the Key Stone + Absolite → `grantKeyStone()` sets `hasKeyStone`, adds a **Lv.30 Absol with perfect IVs holding the Absolite** (Night Slash/Quick Attack/Bite/Slash; to the Box if the party's full, `story.absolToBox`), then **Mabosso's call** (K4: deep resonance, "it comes *back*", E. Voss stopped the drill, "don't tell Dario yet") → `keyStone`, `mabossoExplained`. Alma's Key Stone line was already written. The Boiler Tunnels stay enterable after the blackout (Kilns entry now gates on `blackout` only); the door decor is 🔒 before / 🚪 after.
  - **Moved/removed**: `hasKeyStone` is no longer granted on the Dario win (new win line); **Mega Stones are no longer sold** (badge-5 Mart tier is Max Potion/Max Revive/Leftovers/Linking Cord). Stones now come only from beats/caves/quests per `MEGA.md` — so far Absolite (K3) and Aggronite (Boiler alcove) exist in-game; the other ten are still to be placed as their quests/districts get built.
  - Playwright-validated (`test-keystone.mjs`): wild mons carry rolled IVs; the beat grants the Key Stone + a perfect-IV Absol whose stats match the formula by hand (Lv.30: 88 HP / 92 Atk / 59 Spe); Mabosso's call; the Absol Mega Evolves in the next wild battle (Magic Bounce shown) and reverts after.
- Phase 19 (done): **Signal District** — the first district built *with its cast and beats from day one* (the pattern going forward: design doc → map + scene + zone table + exit gating + NPCs from `CHARACTERS.md` + the district's STORY.md beats, all in one phase). `SIGNAL_MAP` (Ember's mill-block shape, `floorKey 'floorIndoor'`/`blockedKey 'wallStone'`), `SignalScene` (Signal Tower = league idx 3 Prism; the Relay = Mart; the Data Centre → the Risers once `signalJuno`; the Antenna Farm; the Sprawl Bridge locked until Act 3; the Service Stair down), reached from the Greenline Service Stair gated on Thistle's badge. **`RisersScene`** (`RISERS_MAP`, the Cable Risers: Boiler-style corridor, two Meridian Technician chokepoints `techOduya`/`techBrann`, a cold blue wash, `WILD_ZONE_TABLE.risers`) ends at the signal terminal where Priya and Prism (NPCs at the top, `when: !pulseDecoded`) run `RISERS_TERMINAL` — **S2: "That's not weather. That's a *pulse*."** → `pulseDecoded`. **S3**: Dario (`darioSignal`, Lv.30–31, Honchkrow/Gabite/Raichu) and Halloran appear at the Antenna Farm; his line changes if the player has the Key Stone ("…What's that on your wrist"); win → `signalDario`, his winMsg has him staring at the Absol's stone. Cast: Juno (S1, `signalJuno`), Marchetti, Ash & Ren (open the Relay), Busker Talia (her side-quest hook). Roster (`WILD_ZONE_TABLE.signal`): Elekid, Joltik, Klink, Pawniard, Mareep, Electrike (rare), Skarmory (rarer) + Magnemite shared — real stats/dex IDs/abilities/level evolutions (Electabuzz, Galvantula, Klang, Bisharp, Flaaffy→Ampharos, Manectric). **Mega Ampharos / Mega Manectric** added (IDs 10045/10055 verified visually) with Ampharosite/Manectite (not sold) — **Mega roster now 14**. Three real moves added (Spark, Thunder Wave, Steel Wing). Playwright-validated end to end (`test-signal.mjs`): stair gate, Juno, Risers chokepoints, the pulse cutscene, Dario's sponsored battle, Prism's badge → `leagueBeaten[3]`.
  - **Placement rule reminder** (bit the test, not the game): Signal's row 1 has the Relay keepers at (5,1), so row 1 isn't a lateral route — every landmark there has its own column gap (x=1/4/7 on rows 2 and 4). Same for Ember. Route via rows 5–7.
- Phase 20 (done): **the quest system, the ferry quest, and the Undercity — Act 2 is complete end to end.**
  - **Quests** (`src/quests.js`, `scenes/QuestsScene.js`): `QUESTS` registry (name/giver/district/summary/steps/reward); status in `state.quests[key]` ('active' | 'done') set by a new script step `{ quest: { key, status } }`; progress is read off ordinary story flags (each step has a `flag`), so NPC scripts stay flag-based. The Quests overlay (in `OVERLAY_SCENES`) lists active/done quests with the first unmet objective; a **Quests** button sits on every district bar (Harbor/Ember/Greenline/Signal/Old Lines/Undercity).
  - **"The Ferry That Never Left"** (SIDEQUESTS.md #2) is the first quest: Rossi at Badge 4 (`ferryAsked`, quest active) → Kettering lends his crew (`ferryCrew`) → Rossi runs the pumps (camera shake via `HarborScene.drainStair()`), gives the **Gyaradosite**, `stairDrained`, quest done. The Drowned Stair tile then leads down.
  - **The Old Lines** (`OldLinesScene`, `OLDLINES_MAP`, `WILD_ZONE_TABLE.oldlines`): Boiler-style corridor from the drained stair (bottom) up to the hub (top), two Tunnel Dweller chokepoints (`dwellerNyx`, `dwellerCass`), the Warden's shrine alcove with the **Houndoominite** (`OLDLINES_SHRINE`, chalk tallies in groups of twelve, eighteen groups), a deep dark wash. Coming back down from the hub sets `pos.oldlines` to the top entry.
  - **The Undercity** (`UndercityScene`, `UNDERCITY_MAP`, Ember's shape, `WILD_ZONE_TABLE.undercity`): Halvard's Platform (Halvard NPC, chalk years, notices the shrine find), Kestrel's Post (Kestrel opens the Mart; the Post tile is a Mart tile too), **Obsidian's Vault** (league idx 4; **re-stepping the door after the badge runs `OBSIDIAN_AFTER`: the Gengarite + the Warden's name**, `gengariteGiven`), the Warden's Reach, the Underlight hatch (sealed until Act 3). **`WARDEN_BEAT` (U3)**: Elena Voss (NPC on the Reach tile, `when: leagueBeaten[4] && !act2Close`) tells the whole history — deep resonance, the drill, the transformation, "no bond, no way back", eighteen years — asks about Dario (a choice), then the rig's first stage shakes the tunnels: "Bring my son, if he'll come." → `wardenMet`, `act2Close`. Roster: Zubat, Sableye, Drilbur, Koffing, Shuppet, Mawile (+ Gastly/Murkrow shared), real data/evolutions (Golbat, Excadrill, Weezing, Banette). **Mega Sableye / Mega Banette / Mega Mawile** added (IDs 10066/10056/10052 verified visually) with stones not sold — **Mega roster now 17**; Shadow Sneak added. Playwright-validated end to end (`test-undercity.mjs`): quest active → done with the stone, Quests overlay, stair → Old Lines chokepoints + shrine, hub, Obsidian's badge (all five now) + Gengarite, the Warden beat → `act2Close`.
  - **Stones placed in-game so far**: Absolite (K3), Aggronite (Boiler), Gyaradosite (ferry quest), Houndoominite (Old Lines shrine), Gengarite (Obsidian). Still to place per `MEGA.md`: Lucarionite/Gardevoirite/Heracronite/Alakazite (side quests #3–#6), Cameruptite/Pinsirite/Garchompite (postgame), plus the newer Ampharosite/Manectite/Sablenite/Banettite/Mawilite (unassigned — good candidates for Signal/Undercity side quests and the Sprawl).
  - Test-driver lessons: the Normal-move stand-in can't hurt Ghosts (use a Dark move for Undercity tests); NPCs block routes, plan paths around them.
- Phase 21 (done): **The Sprawl — Act 3 begins.** `SPRAWL_MAP` (the Ember block shape, `floorKey 'floorIndoor'`/`blockedKey 'wallBrick'`) + `SprawlScene`: no gym. Halloran's Community Office (1,1), the Concourse (Mart), the Rooftops, the Tower Lobby Elevator (7,7), the Sprawl Bridge down. Reached over Signal's bridge once **all five badges + `act2Close`**. **SP1**: Dario at the office door (`darioSprawl`, `when: act2Close && !sprawlDario`) about to sign the five-year contract; a two-option choice (tell him his mother is alive / that Meridian blamed her), he refuses to hear it, **rival battle 4** (`darioSprawl`, Lv.36–37, Honchkrow/Gabite/**Lucario**/Raichu, reward 300) — winMsg: he goes quiet, asks "Where", leaves. **SP2**: Halloran (`halloranSprawl`, `when: act2Close && !towerBadge`) — before the fight she's protective; after, "What did you tell him?" with a choice, "I didn't *know*", and she hands over her **Tower access badge** → `towerBadge`. Stepping on the elevator with the badge sets `towerOpen` (the Tower scene itself is next). **Sloane** (recruiter, (5,3)) runs SIDEQUESTS.md #9 "Sponsored" as a **dialogue-only quest** (`quests.sponsored` active → the decline choice → done, `sloaneDecided`). Cast also: Resident Jae (rooftop trainer `residentJae`, `sprawlJae`), Mrs. Oyelaran (the kitchen-window view of Meridian). Roster (`WILD_ZONE_TABLE.sprawl`, 8% rate — a place, not a hunting ground): Eevee, Meowth, Snubbull (→ Granbull), Audino, Kangaskhan (rare), Clefairy; **Mega Audino / Mega Kangaskhan** (IDs 10069/10039 verified visually) with stones not sold — **Mega roster now 19**; Take Down and Pound added. Playwright-validated (`test-sprawl.mjs`): bridge gate, Dario's choice + battle → `sprawlDario`, Halloran → `towerBadge`, Sloane → quest done, elevator → `towerOpen`.
- Phase 22 (done): **Act 3's end — the game is now playable from the title screen to the credits.**
  - **`TowerScene`** (`TOWER_MAP`, a 5×12 core corridor, `floorIndoor`/`wallStone`): the lobby elevator (from the Sprawl's elevator with `towerBadge`), Site Security Rask and Executive Marlowe at the floor chokepoints (`secRask`, `execMarlowe` — "I apologise in advance for winning"), Amara beside the office (lines change through the beats), and **Vance's office** (2,0): **`VANCE_TALK` (T2)** — the desk fragment glows near the Key Stone, his true-believer speech, a choice ("It's not a what. It's a who." / "You can't harness a person."), then the existing `ctx 'vance'` battle via the `battle` step's new `kind` option. On return, **`TOWER_RIG` (T3)** runs from `create()`: "Well argued. It changes nothing." — white-out (`rigFlash`), `rigActivated`; the office tile then leads to the **`DescentScene`** (V1: nine strata named in turn with flickers and shakes, skippable) → **`UnderlightScene`** (`UNDERLIGHT_MAP`, 7×7, a green rig-glow wash, Verdanyx as a large 🐲 glyph breathing at (3,1) — `addMonSpriteAt` now accepts `id: null` for custom species). **V2**: Dario blocks the way (`darioUnder`), the one-second mother-and-son scene ("Later."), no pin, Halloran's stone was a gift — `showDarioMega()` places a Mega Lucario sprite; then he stands aside (`darioUnderAfter`). Elena and Priya at the controls. **V3**: the rig tile runs `UNDERLIGHT_RIG` → `ctx 'verdanyx'` battle framed as *holding it until Priya cuts the feed*. **V4 `ENDING`** runs on return: the rig dies (`rigDies`, glow out), Elena takes the Key Stone, the revert (`revertStart/End`, flash sequence, the glyph settles), "Keep the stone. It knows you now.", Dario: "Mum. …Later?" / "No. Now.", the truth surfaces, Vance resigns by hand → `ending` → **`CreditsScene`**.
  - **`CreditsScene`** scrolls the game/story/asset credits and **parses `public/character/CREDITS.md` at build time (`?raw` import)** so the LPC per-asset author/license table is shown in-game — the attribution owed since Phase 7 is now met. Reachable from the ending and from a new **Credits** button on the Title screen (4th menu entry). Space scrolls faster; Back to Title bottom-right.
  - The League hub's tower no longer starts the Vance/Verdanyx battles (it points to the Sprawl elevator); `winBattle` messages for both fit the beats. Engine gap fixed: **`placeNPCs` now re-evaluates `when` after every script** (`refresh()`), so a character whose beat just ended leaves the map immediately (Dario at the Underlight used to keep blocking the rig) and one whose beat just began appears; NPCs spawned after `setupHUD` are HUD-camera-ignored. **Ally/enemy Megas are still narrative-only** (Dario's Mega is a cutscene sprite, not a battle mechanic) — a real double-battle/ally system is postgame-era work.
  - Playwright-validated end to end (`test-ending.mjs`, 11 checks): elevator → Tower, both floor gates, Vance talk → battle → rig, Descent → Underlight, Dario's Mega, the rig fight → `verdanyxBeaten`, the ending → Credits, Credits from the Title.
- Phase 23 (done): **presentation pass, part 1 — battle animations and banners.** The engine now emits `'anim'` events alongside `'render'` (`intro {wild,name}`, `hit {attacker,defender,effectiveness}`, `faint {side}`, `mega`, `ball`, `catch {caught}`), and `BattleScene.onAnim` turns them into motion: each `MonCard`'s sprite lives in a **container** (`holder`, reset via `resetHolder()` whenever the shown mon changes) that **lunges** toward the target (110ms yoyo) while the defender **flashes** (alpha blink ×3) and **shakes**, with a camera shake on super-effective hits; **faints** drop 44px and fade; **HP bars drain** over 420ms (tween on `hpFill.width`) instead of snapping; the **intro** slides the enemy in from off-right with a "wants to battle!" / "A wild X appeared!" banner; **Mega Evolution** white-flashes (`flashRect`), pulses the player sprite 1.35×, shakes, and shows a gold "MEGA EVOLUTION" banner; the **catch** sends a ball text in an arc to the enemy, shrinks the enemy into it, wobbles it (angle ±22° ×6), then sparkles on a catch or pops the enemy back on a break-free — the engine's catch resolution moved from 900ms to **1900ms** to give the wobbles room. **`src/banner.js` `showBanner(scene, text, opts)`**: a dark strip that slides in from the right, holds, slides out; `setupHUD(scene, hudObjects, { banner })` shows an **area-name banner** on entry to every map (each scene passes its header text), and `BattleScene.onEnd` shows a **"🏅 <Leader> Badge earned!"** banner on a league win (the `end` payload now carries `leaderName`). Every banner/anim pushes its type onto `window.__zauAnims` so Playwright can assert on presentation (`test-anim.mjs`: Town/Harbor banners, intro, hit+faint, ball+catch, mega, badge).
  - Lesson: a banner's slide distance must be computed from `GAME_W / 2`, not from the strip's own width (first version landed right-aligned).
- Phase 24 (done): **presentation pass, part 2.** **`EvolutionScene`** (key `'Evolve'`, not in `OVERLAY_SCENES` so it survives a battle ending underneath; holds the input lock): "What? X is evolving!", the sprite goes black and pulses faster and faster (6 pulses, 420ms→120ms), a white flash, the reveal with a gold "Y!" banner, Space/Enter/click to close. Launched by `BattleScene` on the engine's new `evolve` anim event (emitted from `levelUpMon` when `currentMonDisplay(mon).species` changes across `evolveIfReady`) and by `BagScene.applyEvolveItem` for Linking Cord evolutions (the Bag closes underneath). The sequence starts on the glyph immediately and the artwork swaps in whenever it arrives — **a cutscene must never wait on a network**. `loadMonSprite` gained a **4s watchdog** (a reset connection could leave Phaser's loader waiting forever, which is why sandbox screenshots always showed the emoji fallback — production loads the art). **Move-effect layer**: `data/typeColors.js` (the conventional per-type palette) and a type-coloured circle burst on the defender for every hit (`hit` events now carry `moveType`; logged as `fx:<Type>`). **Pause menu** (`PauseScene`, `src/pause.js` `enablePauseMenu`, wired into `setupHUD` so every map has it, plus Home): Escape or M → Party / Bag / Pokédex / Quests / Save / Resume / Back to Title, with the trainer's name, money and badge count. **Trainer spotting** (`npcs.js` `spot()`): an NPC whose script ends in a `battle` pops a gold "!" and pauses ~650ms before speaking (logged `spot`). Playwright-validated (`test-present2.mjs`): pause open/save/resume, hit + `fx:Normal`, Magikarp→Gyarados with the sequence (`evolve:reveal` then close), Kadabra→Alakazam from the Bag, Dario's "!" at the Harbor.
- Phase 25 (done): **evolution stones + the four remaining Mega-Stone side quests.** `data/evolutions.js` entries may now be an **array** (several item evolutions per species — Gloom, Eevee); `evolutionFor()` returns the level entry (or the sole item entry), new **`itemEvolutionsFor()`** lists every item-triggered one; `evolveWithItem` and the Bag's picker use it. Eight real stones (`category 'evolution'`, sold from badge 3 like the real department stores, 💠): Fire/Water/Thunder/Leaf/Sun/Moon/Dusk/Shiny; real stone evolutions: Lampent→Chandelure, Magneton→Magnezone, Gloom→Vileplume|Bellossom, Floette→Florges, Eevee→Vaporeon|Jolteon|Flareon, Murkrow→Honchkrow, Clefairy→Clefable (9 new species with real data). Side quests on the quest system with existing NPCs: **#3 The Last Line** (Kettering after `lineRestored`: Bo's relay → Ila, who quit, now at (1,6) by the haul road → the Furnace Torkoal battle → **Lucarionite**); **#4 What Grows There** (Sato after `stormSeen` → Dr. Kess runs three escalating "Overgrown" battles at the intake — Oddish/Gloom/**Vileplume** → **Gardevoirite**); **#5 The Orchard Bet** (Priya on the Allotments runs Tomas → Pip → **Grandma Osei** as sequential battles → **Heracronite**); **#6 The Song She Can't Finish** (Talia in Signal; recording points set flags on the Antenna Farm tile, the Risers terminal after `pulseDecoded`, and the Sprawl Bridge tile → **Alakazite**). `BattleScene.onEnd` now logs `end:<outcome>:<ctx>` to `__zauAnims`. Playwright-validated (`test-stones.mjs`, 9 checks incl. Water Stone → Vaporeon through the Bag with the evolution sequence).
  - **Stones placed in-game now**: Absolite, Aggronite, Gyaradosite, Houndoominite, Gengarite, Lucarionite, Gardevoirite, Heracronite, Alakazite (9 of 19). Unplaced: Garchompite, Cameruptite, Pinsirite (postgame rewards per MEGA.md), Ampharosite, Manectite, Sablenite, Banettite, Mawilite, Audinite, Kangaskhanite.
  - Test-driver lessons: a stand-in Pokémon **must not level up** (`xpNext: 999999`) or the game correctly recomputes its stats from real base stats and the inflated numbers vanish; NPCs must be reachable without crossing a transition tile (Ila's first placement was only reachable through the Kilns entrance).
- **The game's title is decided (user, 2026-09-12): "Pokémon: Zau Storme".** Applied to the title screen ("POKÉMON: ZAU STORME"), `index.html`'s `<title>`, and the credits. The storm/lightning title-screen visuals are still owed (next).
- **Next**: the **storm title screen** for *Pokémon: Zau Storme* (animated rain/lightning, the logo, the menu — the DOM version's storm effects were deliberately not rebuilt until the title existed), then **postgame** (the Skyline, Verdanyx at rest and catchable, leader/rival rematches with Megas — needs enemy Megas in the engine; Cameruptite/Pinsirite/Garchompite as its rewards), placing the seven unassigned stones, field moves, sound/music (still zero audio), and the Pokédex expansion toward 300–400 species.
- Going forward: keep using `npm run dev` + Playwright for iteration; a production build (`npm run build` + `vite preview`) is worth re-checking before any deploy-affecting change, since dev-server behavior and a real static build can diverge (asset paths, etc.).

### Visual style + future mechanic (user direction, applies to all future work)

- **Target look**: GBA/DS-era top-down JRPG, "up to Gen 5" as the explicit reference point. The current Kenney tileset is a pragmatic CC0 stand-in chosen because it's actually reachable from this sandboxed environment (flat-shaded, not true pixel-art dithering) — not a final style commitment. If a closer GBA-style CC0 pixel tileset becomes available later, swapping `public/tiles/*.png` is a contained change.
  - **Do not source actual ripped Pokémon game tile assets** (ROM-hacking/extraction resources), even if asked again — already asked and declined once (the user acknowledged the site is publicly deployed and wanted them anyway; declined because that's a categorically more direct copyright violation than the PokeAPI artwork hotlinking already in use, which is a promotional-art/established-fan-norm case, not extracted proprietary game data). The path stays: closer-styled CC0/properly-licensed tiles, or hand-drawn/commissioned art later.
- **Central battle gimmick: Mega Evolution — built (Phase 11).** The user's intended signature mechanic, now real: 6 species with real Mega forms (Gyarados, Alakazam, Gengar, Lucario, Garchomp, Absol), Key Stone from the story, one Mega Stone held item per species, once per battle. Only Gen 1-6 species ever got Megas in the real games, which is why the Gen 9 starters can't — any future Mega additions come from expanding the roster (`data/megas.js` + a stone in `data/items.js` + the base species' evolution line), never from inventing a Mega for a species that doesn't have one. The user's longer-term ambition of newer Megas from Pokémon Legends Z-A fits the same pattern once those species/lines exist. Open follow-ups: trainer/rival Megas, and a transformation animation/VFX on the battle screen (currently just the name/type/sprite swap + log line).
- **Character customization: base version done (Phase 7).** Real layered sprites with skin tone/hairstyle/hair color/outfit choices, see Phase 7 above. What's still open for a later pass: more hairstyle/outfit options (only a curated subset of the available LPC assets was copied in), unlockable/purchasable cosmetics (ties into the Mart/badge-tier item system). The in-game Credits screen the license terms required exists since Phase 22 (`CreditsScene`, parses `CREDITS.md`).
- **The game's title: "Pokémon: Zau Storme"** (picked by the user, 2026-09-12). "Zau" stays the region's name; the game is *Zau Storme*. The Phaser build's title screen, page title and credits carry it; the DOM version (`zau-region/`) is content-frozen and still says "Zau Region".

### A real bug found and fixed (2026-09-03)

A mon's `.fainted` flag was never actually set to `true` when its HP hit 0, in **both** `zau-region/src/battle.js` and `zau-region-phaser/src/battleEngine.js` — so `loseBattle`'s 40%-HP recovery-on-loss silently never fired (checked in both places: `git log` for `"if (defender.hp <= 0) defender.fainted = true;"` if this needs re-verifying).

### World/story bible (source of truth for content work)

- `zau-region/WORLD.md` — region geography, the 10-layer vertical city stack.
- `zau-region/STORY.md` — the story bible (Meridian excavation, Verdanyx, the cover-up, Vance/Mabosso/Dario's mother).
- `zau-region/CHARACTERS.md` — character depth (Alma, Priya, Dario, Vance, Mabosso, flavor cast).
- `zau-region/districts/harbor.md` — deep-dive template for a single district; more districts get this treatment as they're built out.

### Game mechanics already implemented (in `zau-region-phaser/`, all ported/rebuilt from the original `zau-region/`)

- Real Pokémon base stats, real move data (`data/moves.js`), full 18-type effectiveness chart with immunities, Gen3+-style stat/damage formulas with real 0–31 IVs (EV=0/neutral nature), STAB, Speed-based turn order.
- Real abilities per species (`data/abilities.js`) and held items (`data/items.js`), both gated by League badge count in the Mart, with a working Bag Give/Take flow.
- Real item roster (Poké/Great/Ultra Ball, Potion line, Revives) gated by League badge count, with a working Bag UI.
- `localStorage` save/load, versioned (`{version: 1, ...state}`), mutates `state` in place (never reassigns — other modules hold a live binding).
- Walkable tile-grid maps (Home, Town, Wild Zone Trail, League Map) with real sprite art (PokeAPI official-artwork hotlinked for mons, layered LPC character sprites for the player) and a real font pairing (Silkscreen/Nunito). Continuous held-key movement (not one tile per keypress), a real 4-direction walk-cycle animation, and a zoomed follow-camera on outdoor maps (Town/Trail/League) so they read as a place you walk through rather than a fully-visible game board.
- A free Pokémon Center (heal-on-demand) and a guard against entering battle with a fully-fainted party.

### Attribution

Commits end with the `Co-Authored-By`/`Claude-Session` trailer and PRs end with the `🤖 Generated with Claude Code` footer, per the attribution instructions active in each session — not hardcoded here since the session URL changes.
